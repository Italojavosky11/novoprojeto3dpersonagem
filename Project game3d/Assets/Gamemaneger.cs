using UnityEngine;

public class Gamemaneger : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public static int pontuacao = 0;


    void start() { pontuacao = 0; }

    void update() { }

}
